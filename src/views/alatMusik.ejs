<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <title>Alat Musik</title>
    <!-- link bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <!-- link font-awesome -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
      rel="stylesheet"
    />

    <style>
      body {
        background-color: #f8f9fa;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      h2 {
        font-weight: 700;
        color: #212529;
      }

      /* Form Styling */
      form.contener {
        background: white;
        padding: 2rem;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        max-width: 700px;
        margin-left: auto;
        margin-right: auto;
        transition: box-shadow 0.3s ease;
      }

      form.contener:hover {
        box-shadow: 0 6px 25px rgba(0, 123, 255, 0.3);
      }

      label.form-label {
        font-weight: 600;
        color: #495057;
      }

      input.form-control,
      textarea.form-control {
        border-radius: 8px;
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
      }

      input.form-control:focus,
      textarea.form-control:focus {
        border-color: #0d6efd;
        box-shadow: 0 0 6px #0d6efd88;
        outline: none;
      }

      /* Button styling */
      button.btn-primary {
        font-weight: 600;
        font-size: 1.1rem;
        border-radius: 8px;
        transition: background-color 0.3s ease, box-shadow 0.3s ease;
      }

      button.btn-primary:hover {
        background-color: #0049b7;
        box-shadow: 0 6px 15px rgba(0, 73, 183, 0.5);
      }

      /* Table Styling */
      .table-responsive {
        overflow-x: auto;
        margin-top: 2rem;
      }

      table.table {
        min-width: 1000px;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 4px 15px rgb(0 0 0 / 0.1);
        background: white;
      }

      table thead {
        background-color: #0d6efd;
        color: white;
        font-weight: 600;
      }

      table tbody tr:hover {
        background-color: #e7f1ff;
        cursor: pointer;
        transition: background-color 0.2s ease;
      }

      /* Table images */
      table tbody img {
        border-radius: 8px;
        object-fit: cover;
        height: 60px;
        width: 80px;
        transition: transform 0.3s ease;
      }
      table tbody img:hover {
        transform: scale(1.05);
      }

      /* Action buttons */
      .btn-warning {
        color: #fff;
        background-color: #ffc107;
        border-color: #ffc107;
        transition: background-color 0.3s ease;
      }
      .btn-warning:hover {
        background-color: #e0a800;
        border-color: #d39e00;
        color: #212529;
      }

      .btn-danger {
        transition: background-color 0.3s ease;
      }
      .btn-danger:hover {
        background-color: #a71d2a;
        border-color: #a71d2a;
      }

      /* Modal Styling */
      .modal-content {
        border-radius: 12px;
        box-shadow: 0 4px 25px rgb(0 0 0 / 0.15);
        padding: 1.5rem;
      }
      .modal-header {
        border-bottom: none;
        padding-bottom: 0;
      }
      .modal-footer {
        border-top: none;
        padding-top: 0;
      }
      .modal-title {
        font-weight: 700;
        font-size: 1.4rem;
      }

      /* Responsive tweaks */
      @media (max-width: 768px) {
        form {
          padding: 1rem;
          max-width: 100%;
        }
        .table-responsive {
          margin-top: 1rem;
        }
        table.table {
          min-width: 700px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <a href="/" class="btn btn-secondary mb-3">
        <i class="fa fa-arrow-left"></i> Kembali ke Dashboard
      </a>

      <h2 class="mb-4 text-center">Tambah Alat</h2>
      <form
        class="contener"
        action="/alatMusik"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="mb-3">
          <label for="nama" class="form-label">Nama</label>
          <input name="nama" id="nama" class="form-control" required />
        </div>

        <div class="mb-3">
          <label for="deskripsi" class="form-label">Deskripsi</label>
          <input
            name="deskripsi"
            id="deskripsi"
            class="form-control"
            required
          />
        </div>

        <div class="mb-3">
          <label for="gambar" class="form-label">Unggah Gambar</label>
          <input
            name="gambar"
            id="gambar"
            type="file"
            accept="image/*"
            class="form-control"
          />
        </div>

        <div class="mb-3">
          <label for="suara" class="form-label">Unggah Suara</label>
          <input
            name="suara"
            id="suara"
            type="file"
            accept=".mp3,.wav,.ogg,.mpeg,.m4a"
            class="form-control"
          />
        </div>
        <div class="mb-3">
          <label for="video" class="form-label">Unggah Video</label>
          <input
            name="video"
            id="video"
            type="file"
            accept="video/*"
            class="form-control"
          />
        </div>

        <button class="btn btn-primary" type="submit">Tambah</button>
      </form>

      <!-- <h2 class="mb-4 text-center">Daftar Alat Musik</h2> -->

      <div class="container my-4">
        <h2 class="text-center mb-3 mt-5">Daftar Alat Musik</h2>
        <table class="table table-bordered table-striped">
          <thead class="table-dark">
            <tr>
              <th>Gambar</th>
              <th>Suara</th>
              <th>vidio</th>
              <th>Nama</th>
              <th>Deskripsi</th>
              <th>Aksi</th>
            </tr>
          </thead>
          <tbody>
            <% data.forEach(function(item){ %>
            <tr>
              <td>
                <img
                  src="<%= item.gambar %>"
                  alt=""
                  style="width: 100px; height: auto"
                />
              </td>
              <td><audio src="<%= item.suara %>" controls></audio></td>
              <td>
                <video
                  src="<%= item.video %>"
                  style="width: 200px; height: auto"
                  controls
                ></video>
              </td>
              <td><%= item.nama %></td>
              <td><%= item.deskripsi %></td>
              <td>
                <!-- Tombol Edit -->
                <button
                  class="btn btn-warning btn-sm"
                  data-bs-toggle="modal"
                  data-bs-target="#edit<%= item.id %>"
                >
                  Edit
                </button>

                <form
                  action="/alatMusik/<%= item.id %>?_method=DELETE"
                  method="POST"
                  style="display: inline"
                >
                  <button
                    class="btn btn-danger btn-sm"
                    type="submit"
                    onclick="return confirm('Hapus data ini?')"
                  >
                    Hapus
                  </button>
                </form>
              </td>
            </tr>

            <!-- Modal Edit -->
            <div
              class="modal fade"
              id="edit<%= item.id %>"
              tabindex="-1"
              aria-hidden="true"
            >
              <div class="modal-dialog">
                <div class="modal-content p-4">
                  <h5>Edit Alat</h5>
                  <form
                    class="contener"
                    action="/alatMusik/<%= item.id %>?_method=PUT"
                    method="POST"
                    enctype="multipart/form-data"
                  >
                    <!-- Simpan path yang lama -->
                    <input
                      type="hidden"
                      name="gambar_lama"
                      value="<%= item.gambar %>"
                    />
                    <input
                      type="hidden"
                      name="video_lama"
                      value="<%= item.video %>"
                    />
                    <input
                      type="hidden"
                      name="suara_lama"
                      value="<%= item.suara %>"
                    />

                    <div class="mb-3">
                      <label for="nama" class="form-label">Nama</label>
                      <input
                        name="nama"
                        id="nama"
                        class="form-control"
                        value="<%= item.nama %>"
                        required
                      />
                    </div>

                    <div class="mb-3">
                      <label for="deskripsi" class="form-label"
                        >Deskripsi</label
                      >
                      <input
                        name="deskripsi"
                        id="deskripsi"
                        class="form-control"
                        value="<%= item.deskripsi %>"
                        required
                      />
                    </div>

                    <div class="mb-3">
                      <label for="gambar" class="form-label"
                        >Ganti Gambar (optional)</label
                      >
                      <input
                        name="gambar"
                        id="gambar"
                        type="file"
                        accept="image/*"
                        class="form-control"
                      />
                    </div>

                    <div class="mb-3">
                      <label for="suara" class="form-label"
                        >Ganti Suara (optional)</label
                      >
                      <input
                        name="suara"
                        id="suara"
                        type="file"
                        accept=".mp3,.wav,.ogg,.mpeg,.m4a"
                        class="form-control"
                      />
                    </div>
                    <div class="mb-3">
                      <label for="video" class="form-label"
                        >Ganti Video (optional)</label
                      >
                      <input
                        name="video"
                        id="video"
                        type="file"
                        accept="video/*"
                        class="form-control"
                      />
                    </div>

                    <button class="btn btn-primary" type="submit">
                      Simpan Perubahan
                    </button>
                  </form>
                </div>
              </div>
            </div>

            <% }); %>
          </tbody>
        </table>
      </div>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <% if (typeof swalMessage !== "undefined" && swalMessage.length > 0 &&
    typeof swalIcon !== "undefined" && swalIcon.length > 0) { %>
    <script>
      Swal.fire({
        icon: "<%= swalIcon[0] %>", // success | info | warning | error
        title:
          "<%= swalIcon[0] === 'success' ? 'Berhasil!' : swalIcon[0] === 'error' ? 'Gagal!' : 'Info' %>",
        text: "<%= swalMessage[0] %>",
        timer: 3000,
        showConfirmButton: false,
        toast: true,
        position: "top-end",
      });
    </script>
    <% } %>
  </body>
</html>
