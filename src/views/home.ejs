<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Dashboard Admin</title>

    <!-- Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Font Awesome 6 -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
      }
      .welcome-text {
        font-family: "Amiri", serif;
        font-size: 3rem;
        color: #343a40;
      }
      .card {
        border-radius: 15px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease;
      }
      .card:hover {
        transform: translateY(-5px);
      }
      .card .card-title {
        font-weight: 700;
        color: #495057;
      }
      .card .card-text {
        font-size: 3rem;
        font-weight: 700;
        color: #0d6efd;
      }
    </style>
    <!-- Google Font Amiri for Arabic -->
    <link
      href="https://fonts.googleapis.com/css2?family=Amiri&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark">
      <div class="container">
        <a
          class="navbar-brand fw-bold fs-4 d-flex align-items-center"
          href="<%= url %>"
        >
          <i class="fas fa-drum me-2"></i> Gendang Lombok Partner
        </a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto align-items-center">
            <li class="nav-item me-2">
              <a
                href="<%= url %>gis"
                class="btn btn-primary btn-sm px-3 d-flex align-items-center"
              >
                <i class="fas fa-map-marked-alt me-1"></i> Lihat Peta
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="<%= url %>">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= url %>profile">Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= url %>admin">Admin</a>
            </li>
            <li class="nav-item ms-3">
              <a class="nav-link" href="<%= url %>login/logout" title="Logout">
                <i
                  class="fa-solid fa-arrow-right-from-bracket text-warning"
                ></i>
              </a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container mt-5 pt-5">
      <div class="text-center mb-5">
        <h1 class="welcome-text display-5">السلام عليكم, <%= userName %>!</h1>
        <p class="fs-5 text-secondary">Selamat datang di Dashboard Admin</p>
      </div>

      <div class="row justify-content-center g-4">
        <div class="col-md-4 col-sm-6">
          <div class="card shadow-sm border-0 rounded-4">
            <div class="card-body text-center py-4">
              <div class="mb-3">
                <i class="fas fa-drum fa-3x text-primary"></i>
              </div>
              <h5 class="card-title fw-bold">Jumlah Gendang Beleq</h5>
              <p class="card-text display-6 fw-semibold text-dark">
                <%= totalGB %>
              </p>
              <a href="<%= url %>admin" class="btn btn-outline-primary mt-3">
                Lihat Detail
              </a>
            </div>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
