<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Dashboard</title>

    <!-- link bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- link font-awesome -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
      rel="stylesheet"
    />

    <style>
      body {
        background-color: #f8f9fa;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      h2 {
        font-weight: 700;
        color: #212529;
      }

      /* Form Styling */
      form {
        background: white;
        padding: 2rem;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        max-width: 700px;
        margin-left: auto;
        margin-right: auto;
        transition: box-shadow 0.3s ease;
      }

      form:hover {
        box-shadow: 0 6px 25px rgba(0, 123, 255, 0.3);
      }

      label.form-label {
        font-weight: 600;
        color: #495057;
      }

      input.form-control,
      textarea.form-control {
        border-radius: 8px;
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
      }

      input.form-control:focus,
      textarea.form-control:focus {
        border-color: #0d6efd;
        box-shadow: 0 0 6px #0d6efd88;
        outline: none;
      }

      /* Button styling */
      button.btn-primary {
        font-weight: 600;
        font-size: 1.1rem;
        border-radius: 8px;
        transition: background-color 0.3s ease, box-shadow 0.3s ease;
      }

      button.btn-primary:hover {
        background-color: #0049b7;
        box-shadow: 0 6px 15px rgba(0, 73, 183, 0.5);
      }

      /* Table Styling */
      .table-responsive {
        overflow-x: auto;
        margin-top: 2rem;
      }

      table.table {
        min-width: 1000px;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 4px 15px rgb(0 0 0 / 0.1);
        background: white;
      }

      table thead {
        background-color: #0d6efd;
        color: white;
        font-weight: 600;
      }

      table tbody tr:hover {
        background-color: #e7f1ff;
        cursor: pointer;
        transition: background-color 0.2s ease;
      }

      /* Table images */
      table tbody img {
        border-radius: 8px;
        object-fit: cover;
        height: 60px;
        width: 80px;
        transition: transform 0.3s ease;
      }
      table tbody img:hover {
        transform: scale(1.05);
      }

      /* Action buttons */
      .btn-warning {
        color: #fff;
        background-color: #ffc107;
        border-color: #ffc107;
        transition: background-color 0.3s ease;
      }
      .btn-warning:hover {
        background-color: #e0a800;
        border-color: #d39e00;
        color: #212529;
      }

      .btn-danger {
        transition: background-color 0.3s ease;
      }
      .btn-danger:hover {
        background-color: #a71d2a;
        border-color: #a71d2a;
      }

      /* Modal Styling */
      .modal-content {
        border-radius: 12px;
        box-shadow: 0 4px 25px rgb(0 0 0 / 0.15);
        padding: 1.5rem;
      }
      .modal-header {
        border-bottom: none;
        padding-bottom: 0;
      }
      .modal-footer {
        border-top: none;
        padding-top: 0;
      }
      .modal-title {
        font-weight: 700;
        font-size: 1.4rem;
      }

      /* Responsive tweaks */
      @media (max-width: 768px) {
        form {
          padding: 1rem;
          max-width: 100%;
        }
        .table-responsive {
          margin-top: 1rem;
        }
        table.table {
          min-width: 700px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <a href="/" class="btn btn-secondary mb-3">
        <i class="fa fa-arrow-left"></i> Kembali ke Dashboard
      </a>
      <h2 class="mb-4 text-center">Form Tambah Data Gendang Beleq</h2>

      <form action="/admin" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="nama_gb" class="form-label">Nama Gendang Beleq</label>
          <input
            type="text"
            class="form-control"
            id="nama_gb"
            name="nama_gb"
            required
          />
        </div>

        <div class="mb-3">
          <label for="alamat_gb" class="form-label">Alamat</label>
          <input
            type="text"
            class="form-control"
            id="alamat_gb"
            name="alamat_gb"
            required
          />
        </div>

        <div class="mb-3">
          <label for="nomer_hp" class="form-label">No. HP</label>
          <input
            type="text"
            class="form-control"
            id="nomer_hp"
            name="nomer_hp"
            required
          />
        </div>

        <div class="mb-3">
          <label for="harga" class="form-label">Harga Sewa</label>
          <input
            type="number"
            class="form-control"
            id="harga"
            name="harga"
            required
          />
        </div>

        <div class="mb-3">
          <label for="deskripsi" class="form-label">Deskripsi</label>
          <textarea
            class="form-control"
            id="deskripsi"
            name="deskripsi"
            rows="3"
            required
          ></textarea>
        </div>

        <div class="mb-3">
          <!-- Input lainnya -->
          <label for="gambar" class="form-label">Gambar</label>
          <input
            type="file"
            name="gambar"
            id="gambar"
            class="form-control"
            accept="image/*"
            required
          />
        </div>

        <div class="row">
          <div class="col-md-6 mb-3">
            <label for="latitude" class="form-label">Latitude</label>
            <input
              type="text"
              class="form-control"
              id="latitude"
              name="latitude"
              required
            />
          </div>
          <div class="col-md-6 mb-3">
            <label for="longitude" class="form-label">Longitude</label>
            <input
              type="text"
              class="form-control"
              id="longitude"
              name="longitude"
              required
            />
          </div>
        </div>

        <div class="mb-3">
          <label for="map" class="form-label">Link Google Maps</label>
          <input type="text" class="form-control" id="map" name="map" />
        </div>

        <button type="submit" class="btn btn-primary w-100 text-center">
          Simpan Data
        </button>
      </form>
    </div>

    <div class="container my-4">
      <h2 class="text-center mb-3 mt-5">Data Gendang Beleq</h2>
      <table class="table table-bordered table-striped">
        <thead class="table-dark">
          <tr>
            <!-- <th>ID</th> -->
            <th>Nama</th>
            <th>Alamat</th>
            <th>No. HP</th>
            <th>Harga</th>
            <th>Deskripsi</th>
            <th>Gambar</th>
            <th>Latitude</th>
            <th>Longitude</th>
            <th>Link Map</th>
            <th>Aksi</th>
          </tr>
        </thead>
        <tbody>
          <% if (data && data.length > 0) { %> <% data.forEach(item => { %>
          <tr>
            <!-- <td><%= item.id %></td> -->
            <td><%= item.nama_gb %></td>
            <td><%= item.alamat_gb %></td>
            <td><%= item.nomer_hp %></td>
            <td>Rp.<%= item.harga %></td>
            <td><%= item.deskripsi %></td>
            <td>
              <img src="<%= item.gambar %>" alt="gambar" width="80" />
            </td>
            <td><%= item.latitude %></td>
            <td><%= item.longitude %></td>
            <td><a href="<%= item.map %>" target="_blank">Link Map</a></td>
            <td>
              <button
                class="btn btn-warning btn-sm"
                onclick="updateData('<%= item.id %>')"
              >
                Edit
              </button>
              <button
                class="btn btn-danger btn-sm"
                onclick="deleteData('<%= item.id %>')"
              >
                Hapus
              </button>
            </td>
          </tr>
          <% }) %> <% } else { %>
          <tr>
            <td colspan="11" class="text-center">Data kosong</td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>

    <!-- Modal Edit Data -->
    <!-- Modal Edit Data -->
    <div
      class="modal fade"
      id="editModal"
      tabindex="-1"
      aria-labelledby="editModalLabel"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-lg">
        <form id="editForm" method="POST" enctype="multipart/form-data">
          <!-- 👇 Disini input _method untuk spoof jadi PUT -->
          <input type="hidden" name="_method" value="PUT" />
          <input type="hidden" id="edit-id" name="id" />
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="editModalLabel">Edit Data</h5>
              <button
                type="button"
                class="btn-close"
                data-bs-dismiss="modal"
                aria-label="Close"
              ></button>
            </div>
            <div class="modal-body">
              <div class="mb-3">
                <label for="edit-nama_gb" class="form-label"
                  >Nama Gendang Beleq</label
                >
                <input
                  type="text"
                  class="form-control"
                  id="edit-nama_gb"
                  name="nama_gb"
                  required
                />
              </div>
              <div class="mb-3">
                <label for="edit-alamat_gb" class="form-label">Alamat</label>
                <input
                  type="text"
                  class="form-control"
                  id="edit-alamat_gb"
                  name="alamat_gb"
                  required
                />
              </div>
              <div class="mb-3">
                <label for="edit-nomer_hp" class="form-label">Nomor HP</label>
                <input
                  type="text"
                  class="form-control"
                  id="edit-nomer_hp"
                  name="nomer_hp"
                />
              </div>
              <div class="mb-3">
                <label for="edit-harga" class="form-label">Harga</label>
                <input
                  type="number"
                  class="form-control"
                  id="edit-harga"
                  name="harga"
                />
              </div>
              <div class="mb-3">
                <label for="edit-deskripsi" class="form-label">Deskripsi</label>
                <textarea
                  class="form-control"
                  id="edit-deskripsi"
                  name="deskripsi"
                ></textarea>
              </div>
              <div class="mb-3">
                <label for="edit-gambar" class="form-label">Gambar</label>
                <input
                  type="file"
                  class="form-control"
                  id="edit-gambar"
                  name="gambar"
                  accept="image/*"
                />
              </div>
              <div class="mb-3">
                <label for="edit-latitude" class="form-label">Latitude</label>
                <input
                  type="text"
                  class="form-control"
                  id="edit-latitude"
                  name="latitude"
                />
              </div>
              <div class="mb-3">
                <label for="edit-longitude" class="form-label">Longitude</label>
                <input
                  type="text"
                  class="form-control"
                  id="edit-longitude"
                  name="longitude"
                />
              </div>
              <div class="mb-3">
                <label for="edit-map" class="form-label">Link Map</label>
                <input
                  type="text"
                  class="form-control"
                  id="edit-map"
                  name="map"
                />
              </div>
            </div>
            <div class="modal-footer">
              <button
                type="button"
                class="btn btn-secondary"
                data-bs-dismiss="modal"
              >
                Batal
              </button>
              <button type="submit" class="btn btn-primary">
                Simpan Perubahan
              </button>
            </div>
          </div>
        </form>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      // Kirim data dari server ke global variable di browser
      window.data = <%- JSON.stringify(data) %>;
    </script>
    <script src="/js/script.js"></script>
  </body>
</html>
