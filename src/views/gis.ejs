<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Peta Lokasi Gendang Beleq</title>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Leaflet CSS -->
    <link
      rel="stylesheet"
      href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
    />

    <style>
      body {
        padding-top: 2rem;
        background-color: darkgray;
      }

      #map {
        height: 75vh;
        border-radius: 15px;
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
      }

      .leaflet-popup-content {
        margin: 0 !important;
      }

      .popup-card-img {
        max-height: 150px;
        object-fit: cover;
      }

      .btn-back {
        margin-bottom: 1rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <a href="<%= url %>" class="btn btn-outline-primary btn-back">
        ← Kembali ke Dashboard
      </a>

      <div class="map-title text-center">
        <h1>🥁 Peta Lokasi Gendang Beleq</h1>
      </div>

      <div id="map"></div>
    </div>

    <!-- Leaflet JS -->
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

    <script>
      const dataGB = <%- JSON.stringify(data) %>;
    </script>

    <!-- link js gis -->
    <script src="/js/gis.js"></script>
  </body>
</html>
