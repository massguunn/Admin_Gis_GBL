<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Profil Saya</title>

    <!-- Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Font Awesome 6 -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
      rel="stylesheet"
    />

    <style>
      body {
        transition: background-color 0.9s, color 0.9s;
      }
      body.dark-mode {
        background-color: #121212;
        color: #e0e0e0;
      }

      .navbar-dark-mode {
        background-color: #1e1e1e !important;
        transition: background-color 0.9s;
      }

      .card.dark-mode {
        background-color: #1e1e1e;
        color: #e0e0e0;
        transition: background-color 0.9s, color 0.9s;
      }

      a.text-decoration-none {
        transition: color 0.3s ease, background-color 0.3s ease;
      }

      a.text-decoration-none:hover {
        color: #0056b3;
        text-decoration: underline;
      }

      body.dark-mode a.text-decoration-none {
        color: #81a1c1;
      }

      body.dark-mode a.text-decoration-none:hover {
        color: #a3c1e1;
      }

      /* Profile Image */
      .profile-img {
        width: 250px;
        height: 250px;
        object-fit: cover;
        border: 5px solid #007bff;
        box-shadow: 0 4px 15px rgba(0, 123, 255, 0.5);
        border-radius: 50%;
        margin: 0 auto 20px auto; /* center horizontally with auto margin */
        display: block;
      }

      /* Kontak link style */
      .contact-links a {
        font-size: 1.1rem;
        margin-right: 20px;
        padding: 6px 12px;
        border-radius: 6px;
        display: inline-block;
        transition: background-color 0.3s ease, color 0.3s ease;
      }

      .contact-links a:hover {
        background-color: #007bff;
        color: white !important;
        text-decoration: none;
      }

      body.dark-mode .contact-links a:hover {
        background-color: #4a90e2;
        color: white !important;
      }

      /* Universitas style normal */
      .univ-text {
        font-weight: 600;
        font-size: 1.1rem;
        margin-bottom: 0.5rem;
      }

      /* Universitas style dark mode */
      body.dark-mode .univ-text {
        color: #d2e1ef; /* lebih cerah dan kontras */
      }

      .card p {
        font-size: 1rem;
        line-height: 1.5;
      }
    </style>
  </head>

  <body>
    <nav
      class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow-sm"
      id="navbar"
    >
      <div class="container">
        <a class="navbar-brand fw-bold fs-4" href="<%= url %>">
          <i class="fas fa-drum me-2"></i>Gendang Lombok Partner
        </a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto align-items-center">
            <li class="nav-item">
              <a class="nav-link" href="<%= url %>">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="<%= url %>profile">Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= url %>admin">Admin</a>
            </li>
            <li class="nav-item ms-3">
              <!-- Dark Mode Toggle -->
              <button
                id="darkModeToggle"
                class="btn btn-outline-light btn-sm"
                title="Toggle Dark Mode"
              >
                <i class="fas fa-moon"></i>
              </button>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container mt-5 pt-5 d-flex justify-content-center">
      <section class="mt-4">
        <div
          class="card p-5 text-center dark-mode-card mx-auto"
          style="max-width: 600px"
        >
          <img src="<%= fotoProfil %>" alt="Foto Profil" class="profile-img" />

          <h3 class="mb-3"><%= nama %></h3>
          <p class="text-muted univ-text">Mahasiswa Universitas Hamzanwadi</p>
          <p class="text-muted mb-4 fst-italic">Web Developer / Programmer</p>

          <p>
            Halo! Saya <strong><%= nama %></strong>, developer aplikasi Gendang
            Lombok Partner. Saya bersemangat untuk melestarikan budaya musik
            tradisional lewat teknologi. Di luar coding, saya suka eksplorasi
            hal-hal baru dan terus belajar.
          </p>

          <h5 class="mt-4 mb-3">Kontak</h5>
          <p class="contact-links">
            <a
              href="mailto:<%= email %>"
              class="text-decoration-none"
              target="_blank"
            >
              <i class="fas fa-envelope"></i> Email
            </a>
            <a href="#" target="_blank" class="text-decoration-none">
              <i class="fab fa-linkedin"></i> LinkedIn
            </a>
            <a href="#" target="_blank" class="text-decoration-none">
              <i class="fab fa-github"></i> GitHub
            </a>
          </p>
        </div>
      </section>
    </main>

    <!-- Bootstrap Bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>

    <script>
      const toggleBtn = document.getElementById("darkModeToggle");
      const body = document.body;
      const navbar = document.getElementById("navbar");
      const card = document.querySelector(".card");

      toggleBtn.addEventListener("click", () => {
        body.classList.toggle("dark-mode");
        navbar.classList.toggle("navbar-dark-mode");
        card.classList.toggle("dark-mode");

        // Change icon
        if (body.classList.contains("dark-mode")) {
          toggleBtn.innerHTML = '<i class="fas fa-sun"></i>';
        } else {
          toggleBtn.innerHTML = '<i class="fas fa-moon"></i>';
        }
      });
    </script>
  </body>
</html>
